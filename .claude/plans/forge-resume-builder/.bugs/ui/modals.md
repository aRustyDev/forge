# Modals

## [UserProfile](http://localhost:5173/config/profile)

- [ ] BUG(bottom left button):
  - Current: rendering behind the main views, but over the sidebar.
  - Expected: rendered on the top-level (ie above everything else) everytime
- [ ] BUG(bottom left button): `User` not rendering `UserName`
  - Current: Shows static 'User' String
  - Expected: Render `UserName` set in UserProfile


## [Summary](http://localhost:5173/resumes/summaries)

- [ ] BUG(Summary Entries): should open as an overlay modal

## [Resumes](http://localhost:5173/resumes)

- [ ] BUG(JD Link): Clicking this from a Resume should open the JD in an overlay/popup modal
