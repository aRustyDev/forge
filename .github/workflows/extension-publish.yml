name: Extension Publish

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      skip_store_publish:
        description: 'Skip store publishing (build + release only)'
        type: boolean
        default: true

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Run extension tests
        run: cd packages/extension && bun test

      - name: Build both browsers
        run: cd packages/extension && bun run build

      - name: Package Chrome zip
        run: |
          VERSION=$(jq -r .version packages/extension/manifest.json)
          cd packages/extension/dist/chrome
          zip -r ../../../../forge-job-tools-chrome-v${VERSION}.zip .

      - name: Package Firefox zip
        run: |
          VERSION=$(jq -r .version packages/extension/manifest.firefox.json)
          cd packages/extension/dist/firefox
          zip -r ../../../../forge-job-tools-firefox-v${VERSION}.zip .

      - name: Package source for AMO review
        run: git archive HEAD --prefix=forge-source/ -o forge-job-tools-source.zip

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: extension-packages
          path: |
            forge-job-tools-chrome-v*.zip
            forge-job-tools-firefox-v*.zip
            forge-job-tools-source.zip

  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          name: extension-packages

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            forge-job-tools-chrome-v*.zip
            forge-job-tools-firefox-v*.zip
            forge-job-tools-source.zip
          generate_release_notes: true

  publish-chrome:
    needs: release
    runs-on: ubuntu-latest
    environment: chrome-web-store
    if: startsWith(github.ref, 'refs/tags/v') && !inputs.skip_store_publish
    steps:
      - name: Load Forge metadata
        uses: aRustyDev/load-secrets-action@main
        with:
          environment-id: ${{ secrets.OP_FORGE_ENVIRONMENT_ID }}
          export-env: true
        env:
          OP_SERVICE_ACCOUNT_TOKEN: ${{ secrets.OP_SVC_ACCT_TOKEN }}

      - name: Load Chrome Web Store credentials
        uses: aRustyDev/load-secrets-action@main
        with:
          environment-id: ${{ secrets.OP_CHROME_ENVIRONMENT_ID }}
          export-env: true
        env:
          OP_SERVICE_ACCOUNT_TOKEN: ${{ secrets.OP_SVC_ACCT_TOKEN }}

      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          name: extension-packages

      - name: Upload to Chrome Web Store
        uses: mnao305/chrome-extension-upload@v5.0.0
        with:
          file-path: forge-job-tools-chrome-v*.zip
          extension-id: ${{ env.CHROME_EXTENSION_ID }}
          client-id: ${{ env.CHROME_CLIENT_ID }}
          client-secret: ${{ env.CHROME_CLIENT_SECRET }}
          refresh-token: ${{ env.CHROME_REFRESH_TOKEN }}
          publish: true

  publish-firefox:
    needs: release
    runs-on: ubuntu-latest
    environment: firefox-addons
    if: startsWith(github.ref, 'refs/tags/v') && !inputs.skip_store_publish
    steps:
      - name: Load Firefox Add-ons credentials
        uses: aRustyDev/load-secrets-action@main
        with:
          environment-id: ${{ secrets.OP_FIREFOX_ENVIRONMENT_ID }}
          export-env: true
        env:
          OP_SERVICE_ACCOUNT_TOKEN: ${{ secrets.OP_SVC_ACCT_TOKEN }}

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install web-ext
        run: npm install -g web-ext

      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          name: extension-packages

      - name: Sign and upload to AMO
        run: |
          mkdir -p /tmp/firefox-ext
          unzip forge-job-tools-firefox-v*.zip -d /tmp/firefox-ext
          web-ext sign \
            --source-dir /tmp/firefox-ext \
            --api-key ${{ env.AMO_API_KEY }} \
            --api-secret ${{ env.AMO_API_SECRET }} \
            --channel listed
